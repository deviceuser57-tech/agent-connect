# 🧠🛡️ CMACK SYSTEM CONTRACT & ARCHITECTURE

**System Name:** CMACK (Cognitive Mode-Aware Causal Kernel)  
**Governance Version:** GRAVITY v1.6 (Self-Constitutional)  
**Database Schema Version:** Phase 10 (Migration v17)  
**Status:** DETERMINISTIC CORE / ADAPTIVE OVERLAY ACTIVE

---

## 🏗️ 1. GRAVITY GOVERNANCE LAYERS (IMMUTABLE STACK)

The following layers MUST exist and be integrated into the `ExecutionInterface`:

| Layer | Version | Responsibility | Status |
|---|---|---|---|
| **Foundation** | v1.0 | Role-based execution control & block logic. | ✅ |
| **Adaptive Intelligence** | v1.2 | Real-time mode switching and threshold tuning. | ✅ |
| **Stability Engine** | v1.3 | Hysteresis control & Drift containment (Safe Fallback). | ✅ |
| **Arbitrator** | v1.4 | Dual-Intelligence arbitration (DNA vs. Governance). | ✅ |
| **Meta-Cognition** | v1.5 | Self-reflection, bias detection, and causality mapping. | ✅ |
| **Self-Constitution** | v1.6 | Dynamic rule generation and policy evolution. | ✅ |

---

## ⚙️ 2. CORE ENGINES (NEVER DELETE / MODIFY CORE LOGIC)

The following engines form the **Deterministic Self-Learning Kernel**. Their internal logic is protected:

1.  **CMACK Kernel (`kernel.ts`)**: The immutable transition handler.
2.  **Orchestrator (`orchestrator.ts`)**: The primary execution loop and GEC validator.
3.  **DNA Engine (`dna-engine.ts`)**: The internal preference and exploration system.
4.  **ESVL Engine (`esvl.ts`)**: The causal simulation and predictive risk system.
5.  **GEC (`gec.ts`)**: Global Execution Controller (Deterministic Safety).

---

## 📂 3. GOVERNANCE MODULES (ADAPTIVE LAYER)

- `governance-engine.ts`: Dynamic rule validation.
- `governance-arbitrator.ts`: Conflict resolution logic.
- `governance-tuner.ts`: Stability and drift control.
- `policy-evolver.ts`: Constitutional self-amendment.
- `governance-reflection.ts`: Post-execution self-evaluation.

---

## 📜 4. MODIFICATION RULES (THE CONTRACT)

1.  **NON-INTRUSIVE OVERLAY ONLY**: All governance logic must live ABOVE the execution interface.
2.  **KERNEL IMMUTABILITY**: DO NOT modify circular logic in the Orchestrator or GEC rules.
3.  **DETERMINISTIC FALLBACK**: Any failure in the adaptive layers must trigger a fallback to `BALANCED_MODE` or `SAFE_RESOLUTION`.
4.  **TRACE INTEGRITY**: Every execution MUST log to `execution_traces` and `governance_traces` with signatures.
5.  **EVOLUTIONARY INTEGRITY**: Rule evolution (v1.6) must not bypass the Stability Window (v1.3).

---

## 📈 5. PERFORMANCE BASELINE

- **Target Determinism**: 100%
- **Target Governance Accuracy**: > 95%
- **Maximum Drift Tolerance**: 0.5
- **Stability Window**: 10 Executions

---

**AUTHORITY:** SYSTEM_CONTRACT_V1_6  
**TIMESTAMP:** 2026-04-23  
**ENFORCED BY:** ARBITRATOR & CONSTITUTION ENGINE
